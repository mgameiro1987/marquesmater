(()=>{
'use strict';
const app=document.getElementById('app');if(!app)return;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});
const api=async(path,opts={})=>{const r=await fetch(path,{cache:'no-store',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok||j.ok===false)throw Error(j.error||`Erro ${r.status}`);return j};
const cfg=p=>p?.options?.__variant_config||null;
const shell=(t,s,b)=>`<section class="page mm106v"><div class="pagehead"><div><h1>${t}</h1><p>${s}</p></div></div>${b}</section>`;
function css(){if(document.getElementById('mm106v-css'))return;const s=document.createElement('style');s.id='mm106v-css';s.textContent=`.mm106v .toolbar{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:14px}.mm106v input,.mm106v select,.mm106v textarea{border:1px solid #d7dee8;border-radius:9px;padding:9px;background:#fff;font:inherit}.mm106v .btn{border:0;border-radius:9px;padding:10px 14px;font-weight:700;cursor:pointer;background:#1677f9;color:#fff}.mm106v .btn.alt{background:#475569}.mm106v .btn.green{background:#16834b}.mm106v .card{padding:18px}.mm106v .tablewrap{overflow:auto}.mm106v table{width:100%;border-collapse:collapse}.mm106v th,.mm106v td{padding:9px;border-bottom:1px solid #e7edf3;text-align:left;vertical-align:middle;white-space:nowrap}.mm106v .muted{color:#64748b;font-size:12px}.mm106v .pill{display:inline-block;padding:4px 8px;border-radius:99px;background:#eef2ff;color:#3730a3;font-size:11px;font-weight:700}.mm106v .editor{display:grid;grid-template-columns:1fr 1fr;gap:14px}.mm106v .full{grid-column:1/-1}.mm106v .box{border:1px solid #e2e8f0;border-radius:12px;padding:13px;background:#f8fafc}.mm106v label{display:flex;flex-direction:column;gap:6px;font-size:12px;font-weight:700;color:#475569}.mm106v .attrrow{display:grid;grid-template-columns:180px 1fr auto;gap:8px;margin:7px 0}.mm106v .combos{margin-top:14px}.mm106v .actions{display:flex;gap:8px;justify-content:flex-end;flex-wrap:wrap;margin-top:15px}.mm106v .modal{position:fixed;inset:0;background:rgba(15,23,42,.6);z-index:10020;display:flex;align-items:center;justify-content:center;padding:14px}.mm106v .modal>div{width:min(1200px,100%);max-height:94vh;overflow:auto;background:#fff;border-radius:18px;padding:20px}.mm106v .help{padding:12px;background:#eff6ff;border-radius:10px;color:#1e40af;font-size:12px;margin-bottom:12px}@media(max-width:800px){.mm106v .editor{grid-template-columns:1fr}.mm106v .full{grid-column:auto}.mm106v .attrrow{grid-template-columns:1fr}.mm106v .modal>div{padding:14px}}
`;document.head.appendChild(s)}
async function products(){const j=await api('/api/catalog?include_inactive=1');return j.catalog||[]}
function normalizeOptions(p){const o=p?.options||{};return Object.entries(o).filter(([k,v])=>!k.startsWith('__')&&Array.isArray(v)&&v.length).map(([name,values])=>({name,values:[...new Set(values.map(String))]}))}
function combinations(attrs){let out=[{}];for(const a of attrs){const next=[];for(const row of out)for(const v of a.values)next.push({...row,[a.name]:v});out=next}return out}
function makeVariantRows(p,existing){const attrs=normalizeOptions(p);const generated=combinations(attrs);const old=existing?.variants||[];return generated.map((opts,i)=>{const found=old.find(v=>Object.keys(opts).every(k=>String(v.options?.[k]??'')===String(opts[k]))&&Object.keys(v.options||{}).length===Object.keys(opts).length);return found||{id:'v'+Date.now()+'_'+i,options:opts,sku:p.sku+(generated.length>1?'-V'+String(i+1).padStart(2,'0'):''),price:Number(p.price||0),oldPrice:null,stockQty:0,stock:'Em stock',weight:0,image:p.image||'',active:true}})}
function variantScreen(){nav();app.innerHTML=shell('Variantes e preços','Gestor universal para todas as categorias: kits, cores, embalagens, tamanhos, capacidades, etc.','<div class="card" style="padding:30px;text-align:center">A carregar catálogo...</div>');products().then(ps=>{app.innerHTML=shell('Variantes e preços','Cada combinação pode ter preço, SKU, stock, peso e imagem próprios.',`<div class="help">Regra central: <b>uma opção não é obrigada a alterar o preço</b>. Ex.: Branco 300 ml e Preto 300 ml podem ter o mesmo preço; a embalagem pode ter preço diferente. Nos kits RIDA, cada combinação pode ter o seu preço próprio.</div><div class="toolbar"><input id="vq" placeholder="Pesquisar produto, SKU ou marca..."><button class="btn" id="vnew">Configurar produto</button></div><div class="card"><div class="tablewrap"><table><thead><tr><th>Produto</th><th>SKU</th><th>Opções</th><th>Variantes</th><th>Preço</th><th>Ação</th></tr></thead><tbody id="vrows">${ps.map((p,i)=>{const c=cfg(p),n=c?.variants?.length||0;return `<tr data-q="${esc((p.name+' '+p.sku+' '+p.brand).toLowerCase())}"><td><b>${esc(p.name)}</b><div class="muted">${esc(p.brand)}</div></td><td>${esc(p.sku)}</td><td>${normalizeOptions(p).map(a=>`<span class="pill">${esc(a.name)}</span>`).join(' ')||'—'}</td><td>${n||'Não configuradas'}</td><td>${money(p.price)}</td><td><button class="btn small" data-v="${i}">Gerir preços</button></td></tr>`}).join('')}</tbody></table></div></div>`);document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>editor(ps[+b.dataset.v]));vnew.onclick=()=>{const p=ps[0];if(p)editor(p)};vq.oninput=()=>document.querySelectorAll('#vrows tr').forEach(r=>r.style.display=!vq.value||r.dataset.q.includes(vq.value.toLowerCase())?'':'none')}).catch(e=>app.innerHTML=shell('Variantes','',`<div class="card">${esc(e.message)}</div>`))}
function editor(p){const old=cfg(p),attrs=old?.attributes||normalizeOptions(p),rows=makeVariantRows(p,old);const m=document.createElement('div');m.className='modal mm106v';m.innerHTML=`<div><h2>Variantes — ${esc(p.name)}</h2><p class="muted">${esc(p.sku)} · ${esc(p.category||'')} · ${esc(p.subcategory||'')}</p><div class="help">Define primeiro as opções. Depois gera as combinações e atribui o preço individual a cada combinação. Se duas combinações tiverem o mesmo preço, basta repetir o valor.</div><div class="box"><b>Opções do produto</b><div id="attrs">${attrs.map(a=>`<div class="attrrow"><input class="aname" value="${esc(a.name)}" placeholder="Ex.: Cor"><input class="avals" value="${esc(a.values.join(', '))}" placeholder="Branco, Preto, Cinza"><button type="button" class="btn alt rm">×</button></div>`).join('')}</div><button type="button" class="btn alt" id="addattr">＋ Adicionar opção</button><button type="button" class="btn" id="regen">Gerar combinações</button></div><div class="combos"><div class="tablewrap"><table><thead><tr><th>Combinação</th><th>SKU</th><th>Preço (€)</th><th>Preço antigo (€)</th><th>Stock</th><th>Peso kg</th><th>Imagem</th><th>Ativo</th></tr></thead><tbody id="combobody"></tbody></table></div></div><div class="actions"><button type="button" class="btn alt" id="cancel">Cancelar</button><button type="button" class="btn green" id="save">Guardar variantes</button></div></div>`;document.body.appendChild(m);const attrsBox=m.querySelector('#attrs');function addAttr(){const d=document.createElement('div');d.className='attrrow';d.innerHTML='<input class="aname" placeholder="Ex.: Cor"><input class="avals" placeholder="Branco, Preto"><button type="button" class="btn alt rm">×</button>';attrsBox.appendChild(d);d.querySelector('.rm').onclick=()=>d.remove()}m.querySelectorAll('.rm').forEach(b=>b.onclick=()=>b.parentElement.remove());m.querySelector('#addattr').onclick=addAttr;function readAttrs(){return [...m.querySelectorAll('.attrrow')].map(r=>({name:r.querySelector('.aname').value.trim(),values:r.querySelector('.avals').value.split(',').map(x=>x.trim()).filter(Boolean)})).filter(a=>a.name&&a.values.length)}function renderRows(){const aa=readAttrs(),comb=combinations(aa),prev=rows;const rr=comb.map((opts,i)=>{const found=prev.find(v=>Object.keys(opts).every(k=>String(v.options?.[k]??'')===String(opts[k]))&&Object.keys(v.options||{}).length===Object.keys(opts).length);return found||{id:'v'+Date.now()+'_'+i,options:opts,sku:p.sku+(comb.length>1?'-V'+String(i+1).padStart(2,'0'):''),price:Number(p.price||0),oldPrice:null,stockQty:0,stock:'Em stock',weight:0,image:p.image||'',active:true}});m._rows=rr;m.querySelector('#combobody').innerHTML=rr.map((v,i)=>`<tr><td>${Object.entries(v.options).map(([k,x])=>`${esc(k)}: ${esc(x)}`).join(' · ')}</td><td><input data-f="sku" data-i="${i}" value="${esc(v.sku)}"></td><td><input data-f="price" data-i="${i}" type="number" step="0.01" value="${Number(v.price||0)}"></td><td><input data-f="oldPrice" data-i="${i}" type="number" step="0.01" value="${v.oldPrice==null?'':Number(v.oldPrice)}"></td><td><input data-f="stockQty" data-i="${i}" type="number" min="0" step="1" value="${Number(v.stockQty||0)}"></td><td><input data-f="weight" data-i="${i}" type="number" min="0" step="0.001" value="${Number(v.weight||0)}"></td><td><input data-f="image" data-i="${i}" value="${esc(v.image||'')}"></td><td><select data-f="active" data-i="${i}"><option value="1" ${v.active!==false?'selected':''}>Sim</option><option value="0" ${v.active===false?'selected':''}>Não</option></select></td></tr>`).join('')}
function collect(){m.querySelectorAll('[data-f]').forEach(x=>{const v=m._rows[+x.dataset.i],k=x.dataset.f;if(k==='price'||k==='oldPrice'||k==='stockQty'||k==='weight')v[k]=x.value===''?(k==='oldPrice'?null:0):Number(x.value);else if(k==='active')v.active=x.value==='1';else v[k]=x.value});return m._rows}m.querySelector('#regen').onclick=renderRows;renderRows();m.querySelector('#cancel').onclick=()=>m.remove();m.querySelector('#save').onclick=async()=>{try{const aa=readAttrs(),rr=collect();if(!aa.length){alert('Cria pelo menos uma opção ou mantém as opções existentes.');return}const nextOptions={...(p.options||{})};aa.forEach(a=>nextOptions[a.name]=a.values);nextOptions.__variant_config={version:1,mode:'combination',attributes:aa,variants:rr};const activePrices=rr.filter(v=>v.active&&Number(v.price)>0).map(v=>Number(v.price));const min=activePrices.length?Math.min(...activePrices):Number(p.price||0);const payload={...p,options:nextOptions,price:min};await api('/api/catalog',{method:'POST',body:JSON.stringify(payload)});alert('Variantes e preços guardados.');m.remove();variantScreen()}catch(e){alert(e.message)}}}
function nav(){document.querySelectorAll('.navitem').forEach(x=>x.classList.toggle('active',x.dataset.section==='variants'))}
document.addEventListener('click',e=>{const b=e.target.closest('.navitem[data-section="variants"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();variantScreen()},true);css();
})();
