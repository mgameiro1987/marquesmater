(()=>{'use strict';if(window.__MM993PE)return;window.__MM993PE=1;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const eur=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});const get=async u=>{const r=await fetch(u,{cache:'no-store'}),j=await r.json();if(!r.ok||j.ok===false)throw Error(j.error||'Erro');return j};const post=async(b)=>{const r=await fetch('/api/purchases',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}),j=await r.json();if(!r.ok||j.ok===false)throw Error(j.error||'Erro');return j};function openDetail(x){const m=document.createElement('div');m.style='position:fixed;inset:0;background:#0f172a99;z-index:100100;display:flex;align-items:center;justify-content:center;padding:12px';m.innerHTML=`<div style="background:#fff;border-radius:16px;padding:20px;width:min(800px,100%);max-height:92vh;overflow:auto"><h2>Compra ${esc(x.number)}</h2><p><b>Fornecedor:</b> ${esc(x.supplier?.name||'—')}</p><p><b>Estado:</b> ${esc(x.status)} · <b>Data:</b> ${esc(x.order_date||'—')} · <b>Prevista:</b> ${esc(x.expected_date||'—')}</p><table style="width:100%;border-collapse:collapse"><thead><tr><th>SKU</th><th>Artigo</th><th>Qtd.</th><th>Custo</th><th>Total</th></tr></thead><tbody>${(x.items||[]).map(i=>`<tr><td>${esc(i.sku)}</td><td>${esc(i.name||'—')}</td><td>${i.quantity}</td><td>${eur(i.cost)}</td><td>${eur(i.total)}</td></tr>`).join('')||'<tr><td colspan="5">Sem artigos.</td></tr>'}</tbody></table><p><b>Subtotal:</b> ${eur(x.subtotal)} · <b>Transporte:</b> ${eur(x.transport)} · <b>Total:</b> ${eur(x.total)}</p><p>${esc(x.notes||'Sem notas.')}</p><button id="mmClose" style="padding:10px 14px;border:0;border-radius:9px">Fechar</button></div>`;document.body.appendChild(m);m.querySelector('#mmClose').onclick=()=>m.remove()}async function enhance(){try{const d=await get('/api/purchases');document.querySelectorAll('[data-r]').forEach(b=>b.style.display='none');const rows=[...document.querySelectorAll('tbody tr')];rows.forEach(tr=>{const btn=tr.querySelector('[data-r]');if(!btn)return;const id=Number(btn.dataset.r);const x=(d.items||[]).find(p=>Number(p.id)===id);if(!x)return;const td=tr.lastElementChild;if(!td||td.querySelector('[data-mm-view]'))return;const v=document.createElement('button');v.textContent='Ver';v.setAttribute('data-mm-view','1');v.className='mm992btn mm992alt';v.onclick=()=>openDetail(x);td.insertBefore(v,td.firstChild)})}catch(e){console.warn('MM993 purchases enhancement',e)}}window.MM993PurchasesEnhance=enhance;setTimeout(enhance,300)})();