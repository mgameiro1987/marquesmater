(()=>{'use strict';if(window.__MMZEROORDERGUARD)return;window.__MMZEROORDERGUARD=1;const MIN=0.01;const money=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});function invalid(v){const n=Number(String(v??'').replace(',','.'));return !Number.isFinite(n)||n<MIN}function guardForm(form){if(!form||form.__mmZeroGuard)return;form.__mmZeroGuard=1;form.addEventListener('submit',e=>{const fields=[...form.querySelectorAll('input,select')].filter(x=>/total|totalprice|amount|preco|price/i.test((x.name||'')+' '+(x.id||'')));if(fields.some(x=>invalid(x.value))){e.preventDefault();e.stopImmediatePropagation();alert('Uma encomenda não pode ter custo zero. Verifique o total da encomenda.');return false}},true)}function scan(){document.querySelectorAll('form').forEach(guardForm)}document.addEventListener('DOMContentLoaded',scan);new MutationObserver(scan).observe(document.documentElement,{subtree:true,childList:true});window.MMZeroOrderGuard={minimum:MIN,validateTotal:v=>!invalid(v),message:'O total da encomenda tem de ser superior a 0 €.'}})();