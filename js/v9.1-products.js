(()=>{'use strict';if(window.__MM91)return;window.__MM91=1;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const money=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});const get=()=>window.MM_V9_CATALOG?.all?.()||window.MARQUES_CATALOG||[];const cats=()=>window.MM_CATEGORIES||[];const subs=c=>window.MM_SUBCATEGORIES?.[c]||[];const save=p=>{if(window.MM_V9_CATALOG?.upsert){window.MM_V9_CATALOG.upsert(p);return true}return false};const shell=(title,sub,body,actions='')=>`<section class="page mm91"><div class="pagehead"><div><h1>${title}</h1><p>${sub}</p></div><div>${actions}</div></div>${body}</section>`;function css(){if(document.getElementById('mm91css'))return;const s=document.createElement('style');s.id='mm91css';s.textContent='.mm91 .mm91bar{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:16px}.mm91 .mm91bar input,.mm91 .mm91bar select{flex:1;min-width:180px}.mm91grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.mm91card{background:#fff;border:1px solid #e5eaf0;border-radius:14px;padding:14px;box-shadow:0 2px 8px #0f172a0a}.mm91img{width:100%;height:150px;object-fit:contain;background:#f8fafc;border-radius:10px}.mm91meta{font-size:12px;color:#64748b;margin:6px 0}.mm91price{font-size:18px;font-weight:800;margin-top:8px}.mm91actions{display:flex;gap:7px;margin-top:12px}.mm91modal{position:fixed;inset:0;background:#0f172a99;display:flex;align-items:center;justify-content:center;padding:16px;z-index:9999}.mm91modalbox{background:#fff;border-radius:16px;width:min(720px,100%);max-height:92vh;overflow:auto;padding:20px}.mm91form{display:grid;grid-template-columns:1fr 1fr;gap:12px}.mm91form .full{grid-column:1/-1}.mm91form label{font-size:12px;font-weight:700;color:#475569}.mm91form input,.mm91form select,.mm91form textarea{width:100%;box-sizing:border-box;margin-top:5px;padding:10px;border:1px solid #d8e0e8;border-radius:8px}.mm91footer{display:flex;justify-content:flex-end;gap:8px;margin-top:16px}@media(max-width:1050px){.mm91grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){.mm91grid{grid-template-columns:1fr}.mm91form{grid-template-columns:1fr}.mm91form .full{grid-column:auto}.mm91img{height:180px}}';document.head.appendChild(s)}function form(p={},idx=-1){const cs=cats();const c=p.category||cs[0]||'';const ss=subs(c);const modal=document.createElement('div');modal.className='mm91modal';modal.innerHTML=`<div class="mm91modalbox"><div style="display:flex;justify-content:space-between;align-items:center"><div><h2 style="margin:0">${idx<0?'Novo produto':'Editar produto'}</h2><small style="color:#64748b">Dados do catálogo V9</small></div><button class="btn" id="mm91close">Fechar</button></div><form class="mm91form" id="mm91form"><label>SKU<input name="sku" required value="${esc(p.sku)}"></label><label>Marca<input name="brand" value="${esc(p.brand)}"></label><label class="full">Nome do produto<input name="name" required value="${esc(p.name)}"></label><label>Categoria<select name="category" id="mm91cat">${cs.map(x=>`<option ${x===c?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label>Subcategoria<select name="subcategory" id="mm91sub">${ss.map(x=>`<option ${x===p.subcategory?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label>Família<input name="family" value="${esc(p.family||'')}"></label><label>Tipo<input name="type" value="${esc(p.type||'')}"></label><label>PVP com IVA (€)<input name="price" type="number" step="0.01" value="${Number(p.price||0)}"></label><label>PVP sem IVA (€)<input id="mm91net" readonly value="${(Number(p.price||0)/1.23).toFixed(2)}"></label><label>Preço anterior (€)<input name="oldPrice" type="number" step="0.01" value="${Number(p.oldPrice||0)}"></label><label>Stock<input name="stock" type="number" step="1" value="${Number(p.stock||0)}"></label><label class="full">Imagem<input name="image" value="${esc(p.image||'')}"></label><label class="full">Descrição<textarea name="description" rows="4">${esc(p.description||'')}</textarea></label><label><input name="active" type="checkbox" ${p.active!==false?'checked':''}> Produto ativo</label><div class="mm91footer full"><button type="button" class="btn" id="mm91cancel">Cancelar</button><button class="btn" type="submit">Guardar produto</button></div></form></div>`;document.body.appendChild(modal);const f=modal.querySelector('form'),price=f.price,net=modal.querySelector('#mm91net');price.oninput=()=>net.value=(Number(price.value||0)/1.23).toFixed(2);modal.querySelector('#mm91cat').onchange=e=>{modal.querySelector('#mm91sub').innerHTML=subs(e.target.value).map(x=>`<option>${esc(x)}</option>`).join('')};const close=()=>modal.remove();modal.querySelector('#mm91close').onclick=close;modal.querySelector('#mm91cancel').onclick=close;f.onsubmit=e=>{e.preventDefault();const d=new FormData(f),o={...p,sku:String(d.get('sku')||'').trim(),brand:String(d.get('brand')||''),name:String(d.get('name')||'').trim(),category:String(d.get('category')||''),subcategory:String(d.get('subcategory')||''),family:String(d.get('family')||''),type:String(d.get('type')||''),price:Number(d.get('price')||0),oldPrice:Number(d.get('oldPrice')||0),stock:Number(d.get('stock')||0),image:String(d.get('image')||''),description:String(d.get('description')||''),active:f.active.checked};if(!o.sku||!o.name){alert('Preenche o SKU e o nome do produto.');return}save(o);close();render()};}function render(){css();const list=get();const body=`<div class="mm91bar"><input id="mm91search" placeholder="Pesquisar por nome, SKU ou marca..."><select id="mm91cat"><option value="">Todas as categorias</option>${cats().map(c=>`<option>${esc(c)}</option>`).join('')}</select><button class="btn" id="mm91new">＋ Novo produto</button></div><div class="mm91grid" id="mm91grid">${list.map((p,i)=>`<article class="mm91card" data-i="${i}" data-text="${esc((p.name+' '+p.sku+' '+p.brand).toLowerCase())}" data-cat="${esc(p.category)}"><img class="mm91img" src="${esc(p.image||'')}" onerror="this.style.visibility='hidden'"><h3 style="margin:10px 0 4px">${esc(p.name)}</h3><div class="mm91meta">${esc(p.brand||'—')} · SKU ${esc(p.sku)}</div><div class="mm91meta">${esc(p.category||'—')} → ${esc(p.subcategory||'—')}</div><div class="mm91price">${money(p.price)}</div><div class="mm91meta">Stock: <b>${esc(p.stock)}</b> · ${p.active===false?'Inativo':'Ativo'}</div><div class="mm91actions"><button class="btn" data-edit="${i}">Editar</button><button class="btn" style="background:#475569" data-del="${i}">Eliminar</button></div></article>`).join('')}</div>`;const app=document.getElementById('app');app.innerHTML=shell('Produtos','Gestor de produtos V9.1 · cartões responsivos e edição no catálogo local V9.',body);const filter=()=>{const q=document.getElementById('mm91search').value.toLowerCase(),c=document.getElementById('mm91cat').value;document.querySelectorAll('#mm91grid article').forEach(x=>x.style.display=(!q||x.dataset.text.includes(q))&&(!c||x.dataset.cat===c)?'':'none')};document.getElementById('mm91search').oninput=filter;document.getElementById('mm91cat').onchange=filter;document.getElementById('mm91new').onclick=()=>form();document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>form(get()[+b.dataset.edit],+b.dataset.edit));document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{const p=get()[+b.dataset.del];if(confirm('Eliminar '+p.name+'?')){window.MM_V9_CATALOG.remove(p.sku);render()}})}const old=window.MMAdmin?.go;window.MMAdmin=window.MMAdmin||{};window.MMAdmin.go=k=>k==='products'?render():old?old(k):null;window.MMAdmin.newProduct=()=>form();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{if(document.querySelector('.navitem[data-section="products"]')){document.querySelectorAll('.navitem[data-section="products"]').forEach(b=>b.addEventListener('click',render))}});})();