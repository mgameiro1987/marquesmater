(()=>{'use strict';
const app=document.getElementById('app');if(!app)return;
const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
const money=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});
const api=async(path,opts={})=>{const r=await fetch(path,{cache:'no-store',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok||j.ok===false)throw Error(j.error||`Erro ${r.status}`);return j};
const cfg=p=>p?.options?.__variant_config||{};
let products=[];let selectedSku='';
function css(){if(document.getElementById('mmv88-css'))return;const s=document.createElement('style');s.id='mmv88-css';s.textContent=`.mmv88{padding:24px;box-sizing:border-box}.mmv88 h1{margin:0;font-size:29px}.mmv88 .sub{margin:6px 0 18px;color:#64748b}.mmv88 .stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:16px}.mmv88 .stat,.mmv88 .panel,.mmv88 .product,.mmv88 .detail{background:#fff;border:1px solid #e2e8f0;border-radius:16px;box-shadow:0 5px 18px rgba(15,23,42,.05)}.mmv88 .stat{padding:16px}.mmv88 .stat small{display:block;color:#64748b;font-weight:700;font-size:11px;text-transform:uppercase}.mmv88 .stat strong{display:block;font-size:25px;margin-top:5px}.mmv88 .toolbar{display:flex;gap:10px;align-items:center;padding:14px;margin-bottom:16px}.mmv88 input,.mmv88 select{box-sizing:border-box;border:1px solid #d7dee8;border-radius:10px;background:#fff;padding:10px 12px;font:inherit;color:#102044}.mmv88 .search{flex:1}.mmv88 .btn{border:0;border-radius:10px;padding:10px 14px;background:#1677f9;color:#fff;font-weight:800;cursor:pointer}.mmv88 .btn.alt{background:#64748b}.mmv88 .btn.green{background:#16834b}.mmv88 .grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.mmv88 .product{padding:15px;display:flex;flex-direction:column;gap:10px}.mmv88 .prodtop{display:flex;gap:12px;align-items:center}.mmv88 .prodtop img{width:64px;height:64px;object-fit:contain;background:#f5f7fa;border-radius:12px;padding:6px}.mmv88 .prodtop h3{margin:0 0 4px;font-size:16px}.mmv88 .muted{color:#64748b;font-size:12px}.mmv88 .meta{display:flex;flex-wrap:wrap;gap:6px}.mmv88 .pill{display:inline-block;padding:5px 8px;border-radius:99px;background:#eef6ff;color:#1d4ed8;font-size:11px;font-weight:800}.mmv88 .prodprice{font-size:18px;font-weight:900}.mmv88 .prodfoot{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:auto;padding-top:8px;border-top:1px solid #edf1f5}.mmv88 .empty{text-align:center;padding:35px;color:#64748b}.mmv88 .detail{padding:16px;margin-top:16px}.mmv88 .detailhead{display:flex;align-items:center;gap:14px;padding-bottom:14px;border-bottom:1px solid #edf1f5}.mmv88 .detailhead img{width:74px;height:74px;object-fit:contain;background:#f5f7fa;border-radius:12px;padding:7px}.mmv88 .detailhead h2{margin:0 0 4px}.mmv88 .tablewrap{overflow:auto;margin-top:12px}.mmv88 table{width:100%;border-collapse:collapse}.mmv88 th,.mmv88 td{padding:11px 12px;border-bottom:1px solid #edf1f5;text-align:left;vertical-align:middle}.mmv88 th{font-size:11px;color:#64748b;text-transform:uppercase}.mmv88 .status{display:inline-block;padding:5px 9px;border-radius:99px;background:#dcfce7;color:#166534;font-size:11px;font-weight:800}.mmv88 .modal{position:fixed;inset:0;background:rgba(15,23,42,.62);z-index:10050;display:flex;align-items:center;justify-content:center;padding:12px}.mmv88 .modal>div{width:min(1100px,100%);max-height:94vh;overflow:auto;background:#fff;border-radius:18px;padding:20px}.mmv88 .box{border:1px solid #e2e8f0;border-radius:12px;padding:14px;background:#f8fafc;margin:12px 0}.mmv88 .attr{display:grid;grid-template-columns:180px 1fr 190px 42px;gap:8px;align-items:center;margin:8px 0}.mmv88 .actions{display:flex;justify-content:flex-end;gap:8px;margin-top:15px}.mmv88 .vtable input{width:100%;min-width:90px}@media(max-width:1050px){.mmv88 .grid{grid-template-columns:repeat(2,minmax(0,1fr))}.mmv88 .stats{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:700px){.mmv88{padding:16px 12px 30px}.mmv88 .grid{grid-template-columns:1fr}.mmv88 .stats{grid-template-columns:repeat(2,minmax(0,1fr))}.mmv88 .toolbar{flex-direction:column;align-items:stretch}.mmv88 .attr{grid-template-columns:1fr}.mmv88 .detailhead{align-items:flex-start}.mmv88 th,.mmv88 td{white-space:normal}}`;document.head.appendChild(s)}
async function load(){const j=await api('/api/catalog?include_inactive=1');products=Array.isArray(j.catalog)?j.catalog:[];return products}
function variantCount(p){const v=cfg(p).variants;return Array.isArray(v)?v.length:0}
function optionCount(p){const a=cfg(p).attributes;return Array.isArray(a)?a.length:0}
function render(){css();const withV=products.filter(p=>variantCount(p)>0).length;const totalV=products.reduce((n,p)=>n+variantCount(p),0);const withO=products.filter(p=>optionCount(p)>0).length;app.innerHTML=`<section class="page mmv88"><h1>Variantes e preços</h1><p class="sub">Gestão de opções, combinações, preços, stock, peso e SKU por produto. Aqui trabalhas sobre o produto real do catálogo central.</p><div class="stats"><div class="stat"><small>Produtos no catálogo</small><strong>${products.length}</strong></div><div class="stat"><small>Com variantes</small><strong>${withV}</strong></div><div class="stat"><small>Com opções configuradas</small><strong>${withO}</strong></div><div class="stat"><small>Variantes configuradas</small><strong>${totalV}</strong></div></div><div class="panel toolbar"><input id="mmv-search" class="search" placeholder="Pesquisar produto, SKU, marca ou categoria..."><select id="mmv-filter"><option value="all">Todos os produtos</option><option value="variants">Com variantes</option><option value="none">Sem variantes</option></select></div><div id="mmv-grid" class="grid"></div><div id="mmv-detail"></div></section>`;document.getElementById('mmv-search').oninput=drawGrid;document.getElementById('mmv-filter').onchange=drawGrid;drawGrid();if(selectedSku)drawDetail()}
function drawGrid(){const q=(document.getElementById('mmv-search')?.value||'').trim().toLowerCase();const f=document.getElementById('mmv-filter')?.value||'all';const list=products.filter(p=>{const text=[p.name,p.sku,p.brand,p.category,p.subcategory,p.type].filter(Boolean).join(' ').toLowerCase();if(q&&!text.includes(q))return false;if(f==='variants'&&!variantCount(p))return false;if(f==='none'&&variantCount(p))return false;return true});const grid=document.getElementById('mmv-grid');if(!grid)return;grid.innerHTML=list.length?list.map(p=>{const vc=variantCount(p),oc=optionCount(p);return `<article class="product"><div class="prodtop"><img src="${esc(p.image||'images/drill.svg')}" alt=""><div style="flex:1"><h3>${esc(p.name||'Sem nome')}</h3><div class="muted">SKU: ${esc(p.sku||'—')}</div></div></div><div class="meta"><span class="pill">${esc(p.brand||'Sem marca')}</span><span class="pill">${esc(p.category||'Sem categoria')}</span>${p.subcategory?`<span class="pill">${esc(p.subcategory)}</span>`:''}</div><div><span class="prodprice">${money(p.price)}</span> <span class="muted">preço base</span></div><div class="muted">${oc} opção${oc===1?'':'ões'} · ${vc} variante${vc===1?'':'s'}</div><div class="prodfoot"><span class="muted">Stock: ${Number(p.stock||p.stockQty||0)}</span><button class="btn" data-manage="${esc(p.sku)}">${vc?'Gerir variantes':'Configurar variantes'}</button></div></article>`}).join(''):`<div class="panel empty" style="grid-column:1/-1">Não existem produtos para os filtros escolhidos.</div>`;grid.querySelectorAll('[data-manage]').forEach(b=>b.onclick=()=>{selectedSku=b.dataset.manage;drawDetail()})}
function drawDetail(){const box=document.getElementById('mmv-detail');if(!box)return;const p=products.find(x=>x.sku===selectedSku);if(!p){box.innerHTML='';return}const c=cfg(p),attrs=Array.isArray(c.attributes)?c.attributes:[],vars=Array.isArray(c.variants)?c.variants:[];box.innerHTML=`<div class="detail"><div class="detailhead"><img src="${esc(p.image||'images/drill.svg')}" alt=""><div style="flex:1"><h2>${esc(p.name)}</h2><div class="muted">SKU ${esc(p.sku)} · ${esc(p.brand||'')} · ${esc(p.category||'')}${p.subcategory?' · '+esc(p.subcategory):''}</div><div style="margin-top:7px"><b>Preço base:</b> ${money(p.price)} · <b>${vars.length}</b> variante${vars.length===1?'':'s'}</div></div><button class="btn" id="mmv-config">⚙ Configurar</button></div>${attrs.length?`<div style="padding-top:12px">${attrs.map(a=>`<span class="pill">${esc(a.name)}: ${esc((a.values||[]).join(', '))}</span>`).join(' ')}</div>`:''}${vars.length?`<div class="tablewrap"><table><thead><tr><th>Variante</th><th>SKU</th><th>Preço</th><th>Stock</th><th>Peso</th><th>Estado</th></tr></thead><tbody>${vars.map(v=>`<tr><td><b>${Object.entries(v.options||{}).map(([k,x])=>`${esc(k)}: ${esc(x)}`).join(' · ')||'Variante'}</b></td><td>${esc(v.sku||'—')}</td><td>${money(v.price)}</td><td>${Number(v.stockQty||0)}</td><td>${Number(v.weight||0)} kg</td><td>${v.active===false?'<span class="status" style="background:#fee2e2;color:#991b1b">Inativa</span>':'<span class="status">Ativa</span>'}</td></tr>`).join('')}</tbody></table></div>`:`<div class="empty" style="padding:24px 10px">Este produto ainda não tem variantes configuradas. Usa <b>Configurar</b> para criar opções e gerar combinações.</div>`}</div>`;document.getElementById('mmv-config').onclick=()=>editor(p);box.scrollIntoView({behavior:'smooth',block:'start'})}
function optionsFromProduct(p){const o=p?.options||{};return Object.entries(o).filter(([k,v])=>!k.startsWith('__')&&Array.isArray(v)&&v.length).map(([name,values])=>({name,values:[...new Set(values.map(String))],mode:'none',prices:{}}))}
function combos(attrs){let rows=[{}];for(const a of attrs){const vals=(a.values||[]).filter(Boolean);if(!vals.length)return[];const next=[];for(const r of rows)for(const v of vals)next.push({...r,[a.name]:v});rows=next}return rows.length===1&&!Object.keys(rows[0]).length?[]:rows}
function calc(base,attrs,o){let p=Number(base||0);for(const a of attrs){const n=Number(a.prices?.[o[a.name]]??0);if(a.mode==='absolute')p=n;else if(a.mode==='delta')p+=n}return Math.max(0,Number(p.toFixed(2)))}
function editor(p){const old=cfg(p);const attrs=(old.attributes||optionsFromProduct(p)).map(a=>({name:a.name,values:[...(a.values||[])],mode:a.mode||'none',prices:{...(a.prices||{})}}));let variants=(old.variants||[]).map(v=>({...v,options:{...(v.options||{})}}));const m=document.createElement('div');m.className='modal mmv88';m.innerHTML=`<div><h2 style="margin-top:0">Configurar variantes</h2><p class="muted"><b>${esc(p.name)}</b> · ${esc(p.sku)} · preço base ${money(p.price)}</p><div class="box"><h3 style="margin:0 0 8px">1. Opções do produto</h3><p class="muted">Ex.: Cor, Capacidade, Bateria, Kit ou Carregador. Cada opção pode não alterar o preço, acrescentar/descontar ou definir preço próprio.</p><div id="mmv-attrs"></div><button class="btn alt" id="mmv-add" type="button">＋ Adicionar opção</button></div><div class="box"><h3 style="margin:0 0 8px">2. Combinações e preços</h3><button class="btn" id="mmv-generate" type="button">Gerar / atualizar combinações</button><span id="mmv-count" class="muted" style="margin-left:10px"></span><div class="tablewrap vtable"><table><thead><tr><th>Combinação</th><th>SKU</th><th>Preço €</th><th>Stock</th><th>Peso kg</th><th>Imagem</th><th>Ativo</th></tr></thead><tbody id="mmv-combos"><tr><td colspan="7" class="empty">Gere as combinações depois de definir as opções.</td></tr></tbody></table></div></div><div class="actions"><button class="btn alt" id="mmv-cancel" type="button">Cancelar</button><button class="btn green" id="mmv-save" type="button">Guardar no catálogo</button></div></div>`;document.body.appendChild(m);const box=m.querySelector('#mmv-attrs');
function renderAttrs(){box.innerHTML=attrs.map((a,i)=>`<div class="attr" data-i="${i}"><input class="an" value="${esc(a.name)}" placeholder="Ex.: Bateria"><input class="av" value="${esc(a.values.join(', '))}" placeholder="2Ah, 4Ah, 6Ah"><select class="am"><option value="none" ${a.mode==='none'?'selected':''}>Não altera preço</option><option value="delta" ${a.mode==='delta'?'selected':''}>Acréscimo / desconto</option><option value="absolute" ${a.mode==='absolute'?'selected':''}>Preço próprio</option></select><button class="btn" style="background:#dc2626;padding:7px 10px" data-del type="button">×</button></div>`).join('')||'<div class="muted">Ainda não existem opções.</div>';box.querySelectorAll('.attr').forEach(d=>{const i=+d.dataset.i;d.querySelector('[data-del]').onclick=()=>{attrs.splice(i,1);renderAttrs()};d.querySelector('.an').oninput=e=>attrs[i].name=e.target.value.trim();d.querySelector('.av').onchange=e=>{attrs[i].values=[...new Set(e.target.value.split(',').map(x=>x.trim()).filter(Boolean))]};d.querySelector('.am').onchange=e=>{attrs[i].mode=e.target.value;renderAttrs()}})}
function renderCombos(){const generated=combos(attrs);const map=new Map(variants.map(v=>[JSON.stringify(v.options||{}),v]));variants=generated.map((o,i)=>map.get(JSON.stringify(o))||{id:'v_'+Date.now()+'_'+i,options:o,sku:p.sku+(generated.length>1?'-V'+String(i+1).padStart(2,'0'):''),price:calc(p.price,attrs,o),oldPrice:null,stockQty:0,weight:0,image:p.image||'',active:true});m.querySelector('#mmv-count').textContent=`${variants.length} combinações`;m.querySelector('#mmv-combos').innerHTML=variants.length?variants.map((v,i)=>`<tr><td><b>${Object.entries(v.options||{}).map(([k,x])=>`${esc(k)}: ${esc(x)}`).join(' · ')}</b></td><td><input data-i="${i}" class="cs" value="${esc(v.sku||'')}"></td><td><input data-i="${i}" class="cp" type="number" step="0.01" value="${Number(v.price||0)}"></td><td><input data-i="${i}" class="cq" type="number" min="0" value="${Number(v.stockQty||0)}"></td><td><input data-i="${i}" class="cw" type="number" step="0.01" value="${Number(v.weight||0)}"></td><td><input data-i="${i}" class="ci" value="${esc(v.image||'')}"></td><td><input data-i="${i}" class="ca" type="checkbox" ${v.active===false?'':'checked'}></td></tr>`).join(''):'<tr><td colspan="7" class="empty">Crie pelo menos uma opção com valores.</td></tr>';m.querySelectorAll('.cs').forEach(x=>x.oninput=e=>variants[+e.target.dataset.i].sku=e.target.value);m.querySelectorAll('.cp').forEach(x=>x.oninput=e=>variants[+e.target.dataset.i].price=Number(e.target.value||0));m.querySelectorAll('.cq').forEach(x=>x.oninput=e=>variants[+e.target.dataset.i].stockQty=Number(e.target.value||0));m.querySelectorAll('.cw').forEach(x=>x.oninput=e=>variants[+e.target.dataset.i].weight=Number(e.target.value||0));m.querySelectorAll('.ci').forEach(x=>x.oninput=e=>variants[+e.target.dataset.i].image=e.target.value);m.querySelectorAll('.ca').forEach(x=>x.onchange=e=>variants[+e.target.dataset.i].active=e.target.checked)}
renderAttrs();m.querySelector('#mmv-add').onclick=()=>{attrs.push({name:'',values:[],mode:'none',prices:{}});renderAttrs()};m.querySelector('#mmv-generate').onclick=renderCombos;m.querySelector('#mmv-cancel').onclick=()=>m.remove();m.querySelector('#mmv-save').onclick=async()=>{try{const clean=attrs.map(a=>({name:a.name,values:a.values,mode:a.mode,prices:a.prices})).filter(a=>a.name&&a.values.length);const data={...p,options:{...(p.options||{}),__variant_config:{attributes:clean,variants,updatedAt:new Date().toISOString()}}};await api('/api/catalog',{method:'POST',body:JSON.stringify(data)});m.remove();await load();render()}catch(e){alert(e.message)}}}
window.MMVariantsRender=async()=>{try{await load();render()}catch(e){css();app.innerHTML=`<section class="page mmv88"><h1>Variantes e preços</h1><p class="sub">Não foi possível carregar o catálogo central.</p><div class="panel empty"><b>${esc(e.message)}</b><br><br><button class="btn" onclick="window.MMVariantsRender()">Tentar novamente</button></div></section>`}};
})();