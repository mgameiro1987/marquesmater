(()=>{'use strict';if(window.__MM1013TAXFRONT)return;window.__MM1013TAXFRONT=1;
const ps=new URLSearchParams(location.search),cat=ps.get('cat')||'Construção';
async function apply(){try{const r=await fetch('/api/taxonomy?v=1031',{cache:'no-store'});const j=await r.json();const c=(j.categories||[]).find(x=>String(x.name)===String(cat));if(!c)return;const order=cat==='RIDA'?(c.families||[]).filter(x=>x.active!==false).sort((a,b)=>(a.order??999999)-(b.order??999999)).map(x=>x.name):(c.subcategories||[]).filter(x=>x.active!==false).sort((a,b)=>(a.order??999999)-(b.order??999999)).map(x=>x.name);const box=document.getElementById('subcats');if(!box)return;const buttons=[...box.querySelectorAll('button')];buttons.sort((a,b)=>{const ai=order.indexOf(a.textContent.trim()),bi=order.indexOf(b.textContent.trim());return (ai<0?999999:ai)-(bi<0?999999:bi)}).forEach(b=>box.appendChild(b));}catch(e){console.warn('Taxonomia: não foi possível aplicar a ordem',e)}} 
const obs=new MutationObserver(()=>apply());obs.observe(document.getElementById('subcats')||document.body,{childList:true,subtree:true});apply();
})();