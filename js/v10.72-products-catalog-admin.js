/* MarquesMater V10.72 — Produtos: método igual ao módulo Categorias */
(()=>{
'use strict';
const app=document.getElementById('app');
if(!app)return;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=n=>Number(n||0).toLocaleString('pt-PT',{style:'currency',currency:'EUR'});
const api=async(path,opts={})=>{const r=await fetch(path,{cache:'no-store',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok||j.ok===false)throw Error(j.error||`Erro ${r.status}`);return j};
function css(){if(document.getElementById('mm72-css'))return;const s=document.createElement('style');s.id='mm72-css';s.textContent=`
.mm72{padding-bottom:30px}.mm72 .toolbar{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:14px}.mm72 input,.mm72 select{border:1px solid #d7dee8;border-radius:9px;padding:10px;background:#fff;font:inherit}.mm72 input{flex:1;min-width:220px}.mm72 .btn{border:0;border-radius:9px;padding:10px 14px;font-weight:700;cursor:pointer;background:#1677f9;color:#fff}.mm72 .btn.alt{background:#475569}.mm72 .grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.mm72 .product{background:#fff;border:1px solid #dfe6ef;border-radius:16px;overflow:hidden;box-shadow:0 3px 12px rgba(16,32,68,.07);min-width:0}.mm72 .photo{height:165px;background:#f6f9fd;display:flex;align-items:center;justify-content:center;padding:12px}.mm72 .photo img{width:100%;height:100%;object-fit:contain}.mm72 .body{padding:13px}.mm72 .name{font-weight:800;font-size:14px;line-height:1.3;color:#102044;min-height:38px;overflow-wrap:anywhere}.mm72 .sku{font-size:10px;color:#64748b;margin-top:4px;overflow-wrap:anywhere}.mm72 .price{font-size:18px;font-weight:900;color:#102044;margin-top:10px}.mm72 .old{font-size:10px;color:#94a3b8;text-decoration:line-through;margin-left:5px}.mm72 .meta{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:11px}.mm72 .meta div{font-size:11px;color:#334155;min-width:0;overflow-wrap:anywhere}.mm72 .meta small{display:block;font-size:8px;font-weight:800;text-transform:uppercase;color:#94a3b8;margin-bottom:3px}.mm72 .pill{display:inline-flex;padding:4px 7px;border-radius:99px;background:#dcfce7;color:#166534;font-size:9px;font-weight:800}.mm72 .pill.off{background:#fee2e2;color:#991b1b}.mm72 .actions{display:flex;gap:6px;margin-top:12px;padding-top:10px;border-top:1px solid #edf1f5}.mm72 .actions .btn{flex:1;padding:8px 7px;font-size:11px}.mm72 .empty{padding:30px;text-align:center;color:#64748b;background:#fff;border:1px solid #dfe6ef;border-radius:14px;grid-column:1/-1}.mm72 .modal{position:fixed;inset:0;background:rgba(15,23,42,.58);z-index:10001;display:flex;align-items:center;justify-content:center;padding:16px}.mm72 .modal form{width:min(900px,100%);max-height:92vh;overflow:auto;background:#fff;border-radius:18px;padding:22px}.mm72 label{font-size:12px;font-weight:700;color:#475569;display:flex;flex-direction:column;gap:6px;margin-bottom:12px}.mm72 label input,.mm72 label textarea,.mm72 label select{box-sizing:border-box;width:100%}.mm72 textarea{min-height:100px;resize:vertical}.mm72 .formgrid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.mm72 .full{grid-column:1/-1}.mm72 .modal-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:15px;flex-wrap:wrap}
@media(max-width:1100px){.mm72 .grid{grid-template-columns:repeat(3,minmax(0,1fr))}}
@media(max-width:700px){.mm72{padding:16px 12px}.mm72 .grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.mm72 .photo{height:120px}.mm72 .body{padding:10px}.mm72 .name{font-size:13px;min-height:34px}.mm72 .price{font-size:16px}.mm72 .meta{gap:6px}.mm72 .toolbar input{min-width:150px;width:100%}.mm72 .formgrid{grid-template-columns:1fr}.mm72 .full{grid-column:auto}}
@media(max-width:360px){.mm72 .grid{grid-template-columns:1fr}}
`;document.head.appendChild(s)}
const shell=(body)=>`<section class="page mm72"><div class="pagehead"><div><h1>Produtos</h1><p>Catálogo central de artigos.</p></div></div>${body}</section>`;
function nav(){document.querySelectorAll('.navitem').forEach(x=>x.classList.toggle('active',x.dataset.section==='products'))}
let state=[];let busy=false;
async function load(){const j=await api('/api/catalog?include_inactive=1');state=j.catalog||[];return state}
function draw(){const grid=document.getElementById('mm72-grid');if(!grid)return;grid.innerHTML=state.map((p,i)=>{const off=p.active===false;return `<article class="product" data-q="${esc(`${p.name||''} ${p.sku||''} ${p.brand||''}`.toLowerCase())}" data-c="${esc(p.category||'')}"><div class="photo"><img src="${esc(p.image||'images/drill.svg')}" alt="${esc(p.name||'')}"></div><div class="body"><div class="name">${esc(p.name||'Sem nome')}</div><div class="sku">SKU: ${esc(p.sku||'—')}</div><div class="price">${money(p.price)}${p.oldPrice!=null?`<span class="old">${money(p.oldPrice)}</span>`:''}</div><div class="meta"><div><small>Marca</small>${esc(p.brand||'—')}</div><div><small>Categoria</small>${esc(p.category||'—')}</div><div><small>Subcategoria</small>${esc(p.subcategory||'—')}</div><div><small>Stock</small>${esc(p.stock||'Em stock')}</div><div><small>Estado</small><span class="pill ${off?'off':''}">${off?'Inativo':'Ativo'}</span></div><div><small>Tipo</small>${esc(p.type||'—')}</div></div><div class="actions"><button class="btn" data-edit="${i}">Editar</button><button class="btn alt" data-toggle="${i}">${off?'Ativar':'Desativar'}</button></div></div></article>`}).join('')||'<div class="empty">Não existem produtos no catálogo.</div>';document.querySelectorAll('#mm72-grid [data-edit]').forEach(b=>b.onclick=()=>editor(state[+b.dataset.edit]));document.querySelectorAll('#mm72-grid [data-toggle]').forEach(b=>b.onclick=async()=>{if(busy)return;busy=true;try{const p=state[+b.dataset.toggle];await api('/api/catalog',{method:'POST',body:JSON.stringify({...p,active:p.active===false})});await render()}catch(e){alert(e.message)}finally{busy=false}})}
function filter(){const q=(document.getElementById('mm72-q')?.value||'').toLowerCase().trim(),c=document.getElementById('mm72-c')?.value||'';document.querySelectorAll('#mm72-grid .product').forEach(x=>x.style.display=(!q||x.dataset.q.includes(q))&&(!c||x.dataset.c===c)?'':'none')}
function editor(p){const x=p||{sku:'',brand:'',name:'',category:'',subcategory:'',type:'',price:0,oldPrice:null,stock:'Em stock',image:'images/drill.svg',badge:'',description:'',technicalSheet:'',active:true,options:{},specs:[]};const m=document.createElement('div');m.className='modal mm72';m.innerHTML=`<form><h2>${p?'Editar artigo':'Novo artigo'}</h2><p style="color:#64748b">Dados gravados no catálogo central PostgreSQL.</p><div class="formgrid"><label>SKU<input name="sku" required value="${esc(x.sku)}"></label><label>Marca<input name="brand" value="${esc(x.brand)}"></label><label class="full">Nome<input name="name" required value="${esc(x.name)}"></label><label>Categoria<input name="category" value="${esc(x.category)}"></label><label>Subcategoria<input name="subcategory" value="${esc(x.subcategory)}"></label><label>Família / Tipo<input name="type" value="${esc(x.type)}"></label><label>Preço (€)<input name="price" type="number" step="0.01" value="${Number(x.price||0)}"></label><label>Preço antigo (€)<input name="oldPrice" type="number" step="0.01" value="${x.oldPrice==null?'':Number(x.oldPrice)}"></label><label>Stock<input name="stock" value="${esc(x.stock||'Em stock')}"></label><label>Estado<select name="active"><option value="1" ${x.active!==false?'selected':''}>Ativo</option><option value="0" ${x.active===false?'selected':''}>Inativo</option></select></label><label class="full">Imagem / URL<input name="image" value="${esc(x.image||'')}"></label><label class="full">Badge<input name="badge" value="${esc(x.badge||'')}"></label><label class="full">Descrição<textarea name="description">${esc(x.description||'')}</textarea></label><label class="full">Ficha técnica / URL<input name="technicalSheet" value="${esc(x.technicalSheet||'')}"></label><label class="full">Opções (JSON)<textarea name="options">${esc(JSON.stringify(x.options||{},null,2))}</textarea></label><label class="full">Especificações (uma por linha)<textarea name="specs">${esc((x.specs||[]).join('\n'))}</textarea></label></div><div class="modal-actions"><button type="button" class="btn alt" data-cancel>Cancelar</button><button class="btn">Guardar artigo</button></div></form>`;document.body.appendChild(m);m.querySelector('[data-cancel]').onclick=()=>m.remove();m.querySelector('form').onsubmit=async e=>{e.preventDefault();if(busy)return;busy=true;const f=new FormData(e.target);let options={};try{options=JSON.parse(String(f.get('options')||'{}'))}catch(err){alert('O campo Opções tem JSON inválido.');busy=false;return}const data={...x,sku:String(f.get('sku')||'').trim(),brand:String(f.get('brand')||'').trim(),name:String(f.get('name')||'').trim(),category:String(f.get('category')||'').trim(),subcategory:String(f.get('subcategory')||'').trim(),type:String(f.get('type')||'').trim(),price:Number(f.get('price')||0),oldPrice:String(f.get('oldPrice')||'').trim()?Number(f.get('oldPrice')):null,stock:String(f.get('stock')||'Em stock').trim()||'Em stock',image:String(f.get('image')||'').trim(),badge:String(f.get('badge')||'').trim(),description:String(f.get('description')||'').trim(),technicalSheet:String(f.get('technicalSheet')||'').trim(),options,specs:String(f.get('specs')||'').split(/\r?\n/).map(v=>v.trim()).filter(Boolean),active:String(f.get('active'))==='1'};try{await api('/api/catalog',{method:'POST',body:JSON.stringify(data)});m.remove();await render()}catch(err){alert(err.message)}finally{busy=false}}}
async function render(){if(busy)return;nav();css();app.innerHTML=shell('<div class="toolbar"><input id="mm72-q" placeholder="Pesquisar por nome, SKU ou marca..."><select id="mm72-c"><option value="">Todas as categorias</option></select><button class="btn" id="mm72-new">＋ Novo artigo</button><button class="btn alt" id="mm72-export">⇩ Exportar catálogo</button></div><div id="mm72-grid" class="grid"><div class="empty">A carregar...</div></div>');try{await load();const cats=[...new Set(state.map(p=>p.category).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt'));document.getElementById('mm72-c').innerHTML='<option value="">Todas as categorias</option>'+cats.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('');draw();document.getElementById('mm72-q').oninput=filter;document.getElementById('mm72-c').onchange=filter;document.getElementById('mm72-new').onclick=()=>editor();document.getElementById('mm72-export').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));a.download='marquesmater-catalogo-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}catch(e){document.getElementById('mm72-grid').innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
function intercept(){document.addEventListener('click',e=>{const b=e.target.closest('.navitem');if(!b)return;if(b.dataset.section!=='products')return;e.preventDefault();e.stopImmediatePropagation();render();document.getElementById('sidebar')?.classList.remove('open')},true)}
css();intercept();
})();
